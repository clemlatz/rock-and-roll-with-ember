{{page-title "Authors"}}

<div class="w-1/3 pix-2">
  <ul class="p1-2 pr-8">
    {{#each @model as |author|}}
      <li class="mb-2">
        <LinkTo @route="authors.author.stories" @model={{author.slug}}>
          {{author.name}}
        </LinkTo>
      </li>
    {{/each}}
  </ul>
</div>
<div class="w-2/3 px-2">
  {{outlet}}
</div>